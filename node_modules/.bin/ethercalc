#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ethercalc/bin/ethercalc" "$@"
  ret=$?
else 
  node  "$basedir/../ethercalc/bin/ethercalc" "$@"
  ret=$?
fi
exit $ret
